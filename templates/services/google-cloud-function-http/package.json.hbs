{
  "name": "{{ serviceName }}",
  "version": "1.0.0",
  "description": "",
  "author": "{{ gitConfig "user.name" }} <{{ gitConfig "user.email" }}>",
  "homepage": "",
  "license": "ISC",
  "main": "lib/index.js",
  "typings": "lib/inde.d.ts",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ],
  "repository": {
    "type": "git",
    "url": "{{ gitConfig 'remote "origin".url' }}"
  },
  "scripts": {
    "clean": "rm -fr ./lib",
    "deploy": "gcloud functions deploy {{ camelCase serviceName }} --memory 128MB --runtime nodejs8 --trigger-http",
    "predeploy": "npm run clean && tsc",
    "test": "echo \"Error: run tests from root\" && exit 1",
    "tslint": "tslint -c ../../tslint.json './src/**/*.ts'"
  },
  "devDependencies": {
    "@types/express": "^4.17.1"
  }
}
